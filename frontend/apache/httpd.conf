ServerRoot "/usr/local/apache2"
Listen 80

LoadModule mpm_event_module modules/mod_mpm_event.so
LoadModule authz_core_module modules/mod_authz_core.so
LoadModule dir_module modules/mod_dir.so
LoadModule mime_module modules/mod_mime.so
LoadModule log_config_module modules/mod_log_config.so
LoadModule unixd_module modules/mod_unixd.so
LoadModule alias_module modules/mod_alias.so
LoadModule rewrite_module modules/mod_rewrite.so
LoadModule proxy_module modules/mod_proxy.so
LoadModule proxy_http_module modules/mod_proxy_http.so

ServerName formula-frontend
DocumentRoot "/usr/local/apache2/htdocs"

User daemon
Group daemon

ErrorLog /proc/self/fd/2
CustomLog /proc/self/fd/1 common

TypesConfig conf/mime.types
DirectoryIndex index.html

<Directory "/usr/local/apache2/htdocs">
    Options Indexes FollowSymLinks
    AllowOverride None
    Require all granted
    RewriteEngine On
    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteCond %{REQUEST_FILENAME} !-d
    RewriteRule ^ index.html [L]
</Directory>

ProxyPass "/api/" "http://formula-api.formula-one-fantasy.svc.cluster.local:8000/api/"
ProxyPassReverse "/api/" "http://formula-api.formula-one-fantasy.svc.cluster.local:8000/api/"

ProxyPass "/health" "http://formula-api.formula-one-fantasy.svc.cluster.local:8000/health"
ProxyPassReverse "/health" "http://formula-api.formula-one-fantasy.svc.cluster.local:8000/health"

